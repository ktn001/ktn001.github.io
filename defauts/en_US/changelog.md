---
layout : default
title : defects
plugin: defauts
lang : en_US
---

# Release Notes

**IMPORTANT**

As a reminder, if there is no information about the update, it means that the update only involves changes to the documentation, translations, or text.

### 21/10/22
Fixed a minor bug in the display of commands
Button to display devices in "table" mode

### 18/10/22
Update to the command interface for Jeedom v4.3

### v1.4.0 (July 16, 2021)
Added orphaned command search.
Jeedom Menu: Analytics => Devices => Orphaned Commands

### V1.3.0 (June 11, 2021)
+ Add setpoint monitoring

### V1.2.1 (June 2, 2021)
+ Align the history records to the left.
+ Selecting the date format for history records.

### V1.2.0 (May 30, 2021)
+ Added a command and its widget to display the history of recent anomalies.

### v1.1.0 ##
+ Improvements to log messages

### V1.0.1 (May 22, 2021)
+ Fixed the appearance of the mouse cursor when hovering over the acknowledgment widget

### V1.0.0 (May 16, 2021)
+ First stable release

### v0.1.0-beta (May 16, 2021)
+ The appearance of the `acquitter` command widget varies depending on the value of the `default` command (the configuration of existing commands is modified when the plugin is updated).
+ By default, the `default` command is not visible (the configuration of existing commands is modified when the plugin is updated).
+ The values of the monitoring commands are inverted for display purposes (the configuration of existing commands is modified when the plugin is updated).

### April 21, 2021 (Beta)
First Publication
